Site header with bold text links (Camplist · Sessions · signout) and the user's name; one per page, full width.

```jsx
<AppHeader userName="Sam Rivera" logoSrc="../../assets/logomark.svg" onNavigate={setRoute} />
```

Links are plain bold text, no underline, no active state. The tent logomark sits left of the bold "Camplist" wordmark.
